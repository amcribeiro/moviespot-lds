<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Tema principal da app -->
    <style name="Theme.MovieSpot" parent="android:Theme.Material.Light.NoActionBar">
    </style>

    <!-- Tema do Splash Screen -->
    <style name="Theme.MovieSpot.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/moviespot_logo</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/black</item>
        <item name="postSplashScreenTheme">@style/Theme.MovieSpot</item>
    </style>

</resources>
